# Isaac Lab livestream controls:
# LIVESTREAM=1 (public Internet) or =2 (LAN/private). Both force headless mode.
LIVESTREAM=1
ENABLE_CAMERAS=1

# Set for public Internet (or leave blank for LAN when LIVESTREAM=2):
#PUBLIC_IP=

# Port knobs (change if you need to avoid conflicts)
WEBRTC_HTTP_PORT=8211
WEBRTC_TCP_PORT=49100
WEBRTC_UDP_PORT=47998